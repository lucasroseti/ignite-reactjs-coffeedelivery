import styled from 'styled-components'

export const DetailContainer = styled.div`
  display: flex;
  align-items: center;
  gap: 0.75rem;

  p {
    font-weight: 400;
    font-size: 1rem;
    line-height: 1.3;
  }

  span {
    font-weight: 600;
  }
`
